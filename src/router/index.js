const userTaskForm = require('./userTaskForm');
const userTask = require('./userTask');
const component = require('./component');
const form = require('./form');

const routes = {
  userTaskForm,
  userTask,
  component,
  form,
};
module.exports = routes;
